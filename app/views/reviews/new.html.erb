<h1> Create a review for <%= @shelter.name %> </h1>
<%= render partial: 'form', locals: { path: "/shelters/#{@shelter.id}/new_review",
                                      method: :post,
                                      button_text: "Create new review"
                                    } %>
